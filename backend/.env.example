# Server Configuration
SERVER_PORT=9090

# Database Configuration (Local Development)
# Using port 5434 to avoid conflicts with other postgres instances
DATABASE_URL=jdbc:postgresql://localhost:5434/tally_db
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# Database Configuration (Supabase - uncomment when ready)
# DATABASE_URL=jdbc:postgresql://db.xxxxx.supabase.co:6543/postgres
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=your_supabase_password

# Security (will be replaced with JWT in Phase 2)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
